typedef long long int integer;


integer solve(int n, int tail, int m, vector<int>& x) {
    
    for(int i = 0; i < (int)x.size(); i++) {
        for(int ptail = 0; ptail < (1<<m); ptail++) {
        }
    }
}
