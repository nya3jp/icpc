#include <cstdio>
#include <cstring>

using namespace std;

char buf[1000000+10];

int main() {
    for(;;) {

        fgets(buf, sizeof(buf), stdin);
        int len = strlen(buf);
        len--;

        int res = 1;
        for(int i = 1; i < len; i++) {
            
        }

    }
    return 0;
}

