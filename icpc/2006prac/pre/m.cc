#include <iostream>
#include <string>
#include <cstdlib>

using namespace std;

int main() {
    vector<string> v(100);
    for(int i = 0; i < 100; i++) {
        cin >> v[i];
    }
    for(int i = 0; i < 50; i++) {
    }
    return 0;
}
